# Yahoo rate-limits datacentre IPs (GitHub Actions) with HTTP 429. The working
# workaround is a curl_cffi session impersonating Chrome (see providers.py).
# Versions pinned for reproducible CI runs; both support impersonate="chrome".
yfinance==0.2.65
curl_cffi>=0.7.4
matplotlib>=3.7
jinja2>=3.1
numpy>=1.24,<2.1
pandas>=2.0,<2.3
requests>=2.31
openpyxl>=3.1
lxml>=4.9
